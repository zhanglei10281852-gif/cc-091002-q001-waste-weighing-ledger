import { validWeight } from './domain.js';
export class LedgerService { constructor(store){this.store=store;} ingest(event){ if(!validWeight(event)) return {status:'rejected',reason:'invalid-weight'}; return {status:this.store.append(event)?'confirmed':'duplicate'}; } current(ticketId){ const events=this.store.all().filter(x=>x.ticketId===ticketId).sort((a,b)=>a.receivedAt.localeCompare(b.receivedAt)); const latest=events.at(-1); if(!latest||latest.action==='revoked') return null; return {ticketId,netKg:latest.grossKg-latest.tareKg,eventId:latest.eventId}; } dailyTotal(){ const tickets=new Set(this.store.all().map(x=>x.ticketId)); return [...tickets].reduce((sum,id)=>sum+(this.current(id)?.netKg??0),0); } }
